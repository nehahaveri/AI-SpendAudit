import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Single Next.js app — audit/summary/leads live as modules under src/lib,
     exposed via API routes. Deploys to Vercel as serverless functions. */
};

export default nextConfig;
